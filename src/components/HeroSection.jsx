import React from "react";

const HeroSection = () => {
  return (
    <section className="bg-gradient-to-r from-blue-500 to-indigo-600 text-white h-64 flex items-center justify-center text-5xl font-extrabold">
      DraftMate
    </section>
  );
};

export default HeroSection;
